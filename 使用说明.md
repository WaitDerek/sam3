# SAM 3.1 物料分割使用说明

本仓库用于用 SAM 3.1 对 `data/` 中的物料图片进行分割检测。

本分支以官方 `sam3.1` 发布分支为基础，当前项目的检测逻辑主要在根目录脚本和 `configs/` 中。

## 目录说明

```text
configs/
  object_segmentation_params.json       # 物料总索引
  materials/<物料名>/*.json              # 按物料、按场景保存的检测参数
  *.txt                                 # 指定图片清单
  material_detection_registry.json       # 修复历史和复现备案
```

输出目录固定为：

```text
out/<结果目录>/
  overlays/     # 可视化结果，*_det.jpg
  masks/        # 二值 mask，*_mask.png
  metadata/     # 检测元数据，*.json
```

模型权重 `sam3.1_multiplex.pt` 放在仓库根目录本地使用，不提交到 Git。

## Label 规则

检测 label 使用纯物料名称，不带数量或数据集编号。

正确：

```bash
python run_material_segmentation.py --label 副雨刮器
```

不推荐：

```bash
python run_material_segmentation.py --label 副雨刮器_1113
```

旧 label 目前作为兼容别名仍可读取，但后续新增配置应使用纯物料名。

## 参数文件

`configs/object_segmentation_params.json` 是总索引。它记录每种物料的输入目录、输出目录、参数快照路径，以及要加载哪些场景参数文件。

例如副雨刮器：

```json
{
  "label": "副雨刮器",
  "parameter_dir": "configs/materials/副雨刮器",
  "parameter_files": [
    "configs/materials/副雨刮器/table_view.json",
    "configs/materials/副雨刮器/bin_view_153324_contact_from_000225.json",
    "configs/materials/副雨刮器/manual_issue_repair.json"
  ]
}
```

`configs/materials/<物料名>/*.json` 是真正控制检测行为的参数文件。一个参数文件对应一个场景或一次修复，例如：

```text
configs/materials/副雨刮器/table_view.json
configs/materials/副雨刮器/bin_view_153324_contact_from_000225.json
configs/materials/副雨刮器/manual_issue_repair.json
```

常见字段：

```json
{
  "name": "table_view",
  "scene": "single_object",
  "sequence_prefixes": ["20260521_151819"],
  "threshold_overrides": {
    "min_score": 0.02,
    "max_instances": 2
  },
  "prompts": [
    "black automotive windshield wiper blade"
  ]
}
```

场景建议：

- `single_object`：单体或台面场景
- `contact`：两个物体接触、重叠、容易合并的场景
- `container`：料箱、托盘、纸箱中检测
- `manual_repair`：用户指定编号的补检或清理

## txt 图片清单

`configs/*.txt` 是“指定图片清单”。这类文件通常用于某次补检、清理、问题编号回放，属于本地一次性运行输入，不提交到 Git。

命名没有硬性要求，只要参数 JSON 中的 `image_list` 路径写对即可。推荐命名方式：

```text
configs/<物料英文或拼音>_<场景或用途>.txt
configs/passenger_wiper_manual_issue_repair.txt
configs/passenger_wiper_table_manual_repair.txt
```

txt 内容每行一个图片 stem、文件名或路径。推荐写 stem：

```text
20260521_151819__000148
20260521_153324__000231
20260521_153324__000232
```

空行会忽略。以 `#` 开头的注释行会忽略：

```text
# table-view pair repair
20260521_151819__000148
```

## 指定几张图片检测

1. 新建图片清单，例如：

```text
configs/passenger_wiper_my_check.txt
```

内容：

```text
20260521_151819__000148
20260521_153324__000231
20260521_153324__000232
```

2. 新建或复制一个参数 JSON，例如：

```text
configs/materials/副雨刮器/my_check.json
```

内容示例：

```json
{
  "name": "my_check",
  "scene": "manual_repair",
  "image_list": "configs/passenger_wiper_my_check.txt",
  "threshold_overrides": {
    "min_score": 0.02,
    "min_area_frac": 0.0003,
    "max_area_frac": 0.06,
    "max_bbox_fill_frac": 0.65,
    "iou_dedup": 0.2,
    "score_threshold_detection": 0.0,
    "new_det_thresh": 0.01,
    "max_instances": 2
  },
  "prompts": [
    "single windshield wiper blade only",
    "black curved rubber wiper blade only"
  ]
}
```

3. 把这个参数文件加入 `configs/object_segmentation_params.json` 中对应物料的 `parameter_files`：

```json
"parameter_files": [
  "configs/materials/副雨刮器/table_view.json",
  "configs/materials/副雨刮器/my_check.json"
]
```

4. 执行：

```bash
python run_material_segmentation.py --label 副雨刮器
```

如果只想快速跑这几张图，也可以直接调用底层脚本，不改总索引：

```bash
python detect_material_bgr.py \
  --input-dir data/副雨刮器总成_1113/bgr \
  --output-dir out/passenger_wiper_1100 \
  --label 副雨刮器 \
  --image-list configs/passenger_wiper_my_check.txt \
  --min-score 0.02 \
  --min-area-frac 0.0003 \
  --max-area-frac 0.06 \
  --max-bbox-fill-frac 0.65 \
  --iou-dedup 0.2 \
  --max-instances 2 \
  --score-threshold-detection 0.0 \
  --new-det-thresh 0.01 \
  --text-prompt "single windshield wiper blade only" \
  --text-prompt "black curved rubber wiper blade only"
```

## 常用命令

按配置跑某个物料：

```bash
python run_material_segmentation.py --label 副雨刮器
```

只生成参数快照，不跑检测：

```bash
python run_material_segmentation.py --label 副雨刮器 --write-params-only
```

跑单张图片：

```bash
python detect_material_bgr.py \
  --image data/副雨刮器总成_1113/bgr/20260521_151819__000148.png \
  --output-dir out/test_single \
  --label 副雨刮器 \
  --text-prompt "black windshield wiper blade"
```

## 维护原则

- 新增场景适配时，优先新增 `configs/materials/<物料名>/<场景>.json`。
- 不要为了一个问题编号改坏已有稳定场景。
- 指定编号的补检、误检清理，优先用 `image_list` 加 txt 清单保存。
- `data/` 和 `out/` 是本地数据与输出，不进入 Git。
